{
    "{{$inline1}}": {
        "$if": "{{$isPojo}}",
        "id": "{{id}}",
        "category": "{{category}}"
    },
    "{{$inline2}}": {
        "$if": "{{$isJSON}}",
        "name": "{{$jpointer:'/name'}}",
        "price": "{{$jpointer:'/price'}}"
    }
}